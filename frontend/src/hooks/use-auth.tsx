// Re-export the AuthContext and useAuth hook from the main AuthContext file
import { AuthContext, AuthProvider, useAuth } from '../contexts/AuthContext';

export { AuthContext, AuthProvider, useAuth };
